기본 logic

처음 진입 했을 때 현재 위치 가져옴.
가져올 수 잇으면 그걸 중심좌표로,
못가져오면 그냥 기본 중심좌표 설정.

그 다음에 607개 부스 가져옴.
이 607개 다 for문 돌면서 map에 다 박기.
	이 과정에서 중심좌표 주변을 찾아서 list up하도록 되어있음.

search로 해서 키워드를 입력하면 새로고침해서 재진입.
이때는 그 키워드를 포함한 부스 n개만 가져옴.
그것도 똑같이 for문 돌면서 map에 다 박고
	그 과정에서 중심좌표 주변만 list up.

=> 현재 위치 사용 안하는 스위치 만들어야 할 듯.

지도를 이동하면(확대축소 or 중심좌표 옮기기)
그 새롭게 옮긴거에 맞게 search하고 list up.

가나다순을 누르면
현재 위치 주변만 list up 한 것들 중 (mapbooth 배열에 담긴것들)
애들끼리 정렬하고 다시 list up. 원래꺼 삭제하고 덮어쓰는 식.

이제 필터를 하면...
원래 print 할때 전부 print.
필터를 클릭 하면, 재 list up하게 할 예정.
그 과정에서 name in boothselected 면 print.



디폴트) 모든 핀 다 찍혀있는 상태, 모든 브랜드 선택

필터 -> 클릭된 브랜드만 띄우기 & 그 브랜드들만 핀 "다시" 찍기


1) 크롤링에 경도위도까지. 좌표변환 안하게.
2) 마커 찍을 때 브랜드 선택했는지 검사 & 색깔까지. 
3) 검색 햇을때 중심좌표 옮기기.
4) 뷰->html->js 말고 뷰->js로 ajax
